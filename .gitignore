node_modules/
dist/
.next/
.turbo/
*.tsbuildinfo
.DS_Store
test-results/
playwright-report/
coverage/
.env
.env.local
.claude/settings.local.json
.claude/worktrees/
coverage-report/
.source/
apps/demo/data/

# UI/UX v5.1 E2E fixture harness (scripts/e2e/) — persistent browser profiles carry
# session cookies/local-storage, screenshots are regenerated per finish-pass round.
.e2e/
docs/ux-log/e2e-latest/
docs/previews/

# Deploy runtime state — the demo server's own log and pid, rewritten on every
# start. Tracked once by mistake; the scripts recreate both.
deploy/logs/
deploy/run/

# Playwright MCP scratch output — snapshots and one-off screenshots taken while
# debugging. Evidence worth keeping goes to docs/ux-log/ by hand.
.playwright-mcp/

# Internal AI-agent planning/process docs — not part of the public project.
/CLAUDE.md
/GOAL.md
/REBRANDING_PLAN.md
/package-install-guide.md
/Caddyfile.instafix
